[package]
name = "store"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = {version = "0.8", features= ["postgres", "runtime-async-std"]}
dotenv = "0.15"
serde = {version = "1.0", features=["derive"]}
serde_json = "1.0"
actix = "0.13.5"
